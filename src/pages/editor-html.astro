---
import ReactHTMLEditor from '@/components/ReactHTMLEditor.tsx'
import Layout from '@/layouts/Layout.astro'
import { MAIN_DESCRIPTION } from '@/utils/consts'
---

<Layout title={`Editor de HTML`} description={MAIN_DESCRIPTION}>
  <div class='flex flex-col gap-5 px-5 min-h-dvh xs:px-10 sm:px-14 xl:max-w-7xl xl:mx-auto'>
    <main class='flex flex-col gap-12 grow mt-36'>
      <header>
        <h1 class='text-4xl font-bold text-center'>Editor de HTML</h1>
        <p class='mt-4 text-lg text-center'>
          Edita tu código HTML y visualiza los cambios en tiempo real.
        </p>
      </header>

      <ReactHTMLEditor client:only />
    </main>
  </div>
</Layout>
