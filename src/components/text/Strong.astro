---
interface Props {
	color?: string
}

const { color } = Astro.props as Props
const textColor = color || 'dark:text-customWhite text-customBlack'
---

<strong class={textColor}>
	<slot />
</strong>
